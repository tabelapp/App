// Plugins são aplicados em cada módulo (versões em gradle/libs.versions.toml).
// Não declaramos nada aqui de propósito: assim o módulo :core compila numa
// máquina sem Android SDK / sem acesso ao repositório Maven do Google.
